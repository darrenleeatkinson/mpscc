server:
  port: 8089
spring:
  application:
    name: call-simulator-service
sim:
  intensity: ${SIM_INTENSITY:1.0}
  intake-url: ${INTAKE_URL:http://call-intake-service:8082}
management:
  endpoints:
    web:
      exposure:
        include: health,info
